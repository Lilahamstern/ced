version: "3.5"

services:
  server:
    build:
      context: ./server
    networks:
      - database
      - server

  web:
    build:
      context: ./web
    networks:
      - server

  db:
    build: ./db
    networks:
      - database

networks:
  server:
    driver: bridge

  database:
    driver: bridge