type Project {
    id: ID!
    version: Version
    createdAt: String!
    updatedAt: String!
}

input creatProjectInput {
    id: ID!
}

type Query {
    projects: [Project!]!
    project(id: ID!): Project!
}

type Mutation {
    createProject(input: creatProjectInput): Project!
}