type Project {
    id: Int!
    versions: [Version]
    createdAt: String!
    updatedAt: String!
}

input creatProjectInput {
    id: Int!
}

type Query {
    projects: [Project]!
    project(id: Int!): Project!
}

type Mutation {
    createProject(input: creatProjectInput): Project!
}