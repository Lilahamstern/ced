type Project {
  id: ID!
  versions: [Version]
  createdAt: String
  updatedAt: String
}

type Query {
  projects: [Project]
  project(id: ID!): Project
  #countProjects: Long!
}

input createProjectInput {
  id: ID!
}

type Mutation {
  createProject(input: createProjectInput): Project!
  #updateProject(id: ID!): Project!
  deleteProject(input: deleteInput!): Boolean!
}
